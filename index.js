const newID = oldID + 1000000000;

const ageIsValid = currentAge.isInteger();

const randomNumber = Math.random(0, 20);

const randomInteger = randomNumber.round;

const randomUserID = randomInteger(1000000001, 1000000020)
